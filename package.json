{
  "name": "@embodi/types",
  "version": "0.1.0",
  "description": "Types for embodi project to develop elements with typescript",
  "main": "types.js",
  "scripts": {
    "build": "tsc --declaration --declarationMap --emitDeclarationOnly --declarationDir ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/svelte": "^5.0.0",
    "@types/node": "^20.4.2",
    "svelte": "^4.0.5",
    "typescript": "^5.1.6"
  }
}
